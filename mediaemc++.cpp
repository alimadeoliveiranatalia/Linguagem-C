#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	int p1, p2;
	float n1, n2, media;
	p1=2;
	p2=3;
	
	printf("Informe a primeira nota, por favor.");
		gets(char*_n1);
	printf("Informe a segunda nota, por favor.");
		gets(char*_n2);
			media = (n1*p1+n2*p2)/(p1+p2);
	printf("A media resultou em: %f", &media);
	
	
	return 0;

